# Environment configuration for PDF Link Extraction Evaluation

# Paths to input directories (absolute or relative)
PDF_DIR=./pdfs
ANNOTATIONS_DIR=./annotations

# Path where the evaluation results will be saved
OUTPUT_JSON=./eval_results.json

# Text similarity threshold for matching PDF references to JSON annotations (0.0 to 1.0)
SIMILARITY_THRESHOLD=0.7
